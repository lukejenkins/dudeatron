# SSH Connection Settings
# Leave these blank to have the script prompt you for credentials
SSH_USERNAME=your_username
SSH_PASSWORD=your_password
SSH_ENABLE_SECRET=your_enable_secret

# Connection Timeout (seconds)
SSH_TIMEOUT=30

# Output Directory for files generated by scripts. e.g. .csv and .json
# Default: current directory
# Example: OUTPUT_DIR=./output/
OUTPUT_DIR=.

# Device Type (optional - set automatically by each script)
# dudeatron.py defaults to: cisco_ios
# dudeatron_wlc.py uses: cisco_wlc_ssh
# Uncomment to override for specific device models:
# DEVICE_TYPE=cisco_ios
